// Bundled for the popup: the WEQ8Runtime class (used for a silent, UI-only
// instance) plus the <weq8-ui> custom element (registers itself on import).
export { WEQ8Runtime } from "weq8";
import "weq8/ui";
